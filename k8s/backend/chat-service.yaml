apiVersion: apps/v1
kind: Deployment
metadata:
  name: chat-service
  namespace: fluxchat
  labels:
    app: chat-service
    app.kubernetes.io/name: chat-service
    app.kubernetes.io/component: microservice
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 0
  selector:
    matchLabels:
      app: chat-service
  template:
    metadata:
      labels:
        app: chat-service
    spec:
      terminationGracePeriodSeconds: 30
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: chat-service
          image: fluxchat-chat-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8003
          envFrom:
            - configMapRef:
                name: fluxchat-config
            - secretRef:
                name: fluxchat-secrets
          env:
            - name: PORT
              value: "8003"
            - name: HOST
              value: "0.0.0.0"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health/live
              port: 8003
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 8003
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 2
---
apiVersion: v1
kind: Service
metadata:
  name: chat-service
  namespace: fluxchat
  labels:
    app: chat-service
    app.kubernetes.io/name: chat-service
spec:
  type: ClusterIP
  selector:
    app: chat-service
  ports:
    - name: http
      port: 8003
      targetPort: 8003
      protocol: TCP
